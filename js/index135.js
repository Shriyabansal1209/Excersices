const mutants = ['Professor X', 'Cyclops', 'Iceman', 'Angel', 'Magneto', 'Beast', 'Phoenix', 'Logan', 'Gambit'];

mutants.forEach(function logArrayElements(mutants, index, array) {
  console.log('a[' + index + '] = ' + mutants);
});

