let numbers = [1];

const numbersMap = numbers.map(function(number) {
    for (numbers = 1; numbers <=1000; numbers++){
        console.log('Index:',numbers, 'Original Number:',numbers, 'Incremented value:',numbers+10)
    }
    
});
