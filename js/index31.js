const firstSide = 10;
const secondSide = 20;
const thirdSide = 5;

const result = firstSide + secondSide + thirdSide;

console.log('Perimeter of triangle is total of all the sides, here the perimeter is', result);